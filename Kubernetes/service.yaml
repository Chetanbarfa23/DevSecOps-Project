apiVersion: v1
kind: Service
metadata:
  name: netflix-service
  labels:
    app: netflix
    project: devsecops-netflix
spec:
  type: NodePort

  selector:
    app: netflix

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
